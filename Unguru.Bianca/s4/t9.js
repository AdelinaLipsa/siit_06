var t9a = function (a, b) {
    var x = 0;
    for (var i = 0; i <= a, i <= b; i++) {
        if (a % i == 0 && b % i == 0) {
            x = i;
        }
    }
    return (x);
    var numar = (a * b) / x;
    console.log(numar)
}

